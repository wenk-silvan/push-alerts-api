namespace PushAlertsApi.Models
{
    /// <summary>
    /// Represents the states in which a task can be.
    /// </summary>
    public enum TaskState
    {
        Opened,
        Assigned,
        Done,
        Rejected,
    }
}